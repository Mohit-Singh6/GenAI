services:
  qdrant_server:
    image: qdrant/qdrant:latest
    container_name: qdrant_db
    ports:
      - "6333:6333"  # HTTP REST API & Web UI Dashboard
      - "6334:6334"  # High-speed gRPC API
    volumes:
      - ./qdrant_storage:/qdrant/storage:z
    restart: unless-stopped